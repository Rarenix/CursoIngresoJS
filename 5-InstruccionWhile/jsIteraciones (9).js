function Mostrar()
{	
	var min = 0;
	var max = 0;
	var primera = true;
	var rta ;
	while (rta == "si")
{	
	contador ++
	num = prompt("inserte numero")
	num = parseInt(numero);
	while (isNaN(numero))
{	
	num = prompt(numero)
	num= parseint(numero)
}   
	if (primera)
{	
	primera = false; 
	max = min;
	min = max;
}		
	else{
}//FIN DE LA FUNCIÓN